import { expect } from 'vitest'
import { isVerifiedUser } from '@/api/user'
import { ethers } from 'ethers'

describe('users', () => {
	it('isVerifiedUser - should throw error if the addesses are invalid', async () => {})
})
